const allInputs = document.querySelectorAll('.input')

const submit = document.getElementById('submit')

submit.addEventListener('click', () => {
    allInputs.firstChild.forEach((curr) => {
        if(curr.textContent === '') {
           
        } else {
            
        }
    })
})